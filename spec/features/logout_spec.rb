require_relative '../spec_helper'

feature 'User log out' do
  context 'after user clicks log out' do
    it 'puts the user on the home page and tells them they are logged out' do
      pending
    end
  end
end
